--- 
title: 'Mosaic' 
tags: ["contents"] 
issueNo: 8
cover: '/images/issue8img/cover8.jpg'
layout: 'issue.njk' 
categories:
  - title: "Poetry"
    tag: "poetry"
  - title: "Prose"
    tag: "prose"
  - title: "Essays"
    tag: "essay"
  - title: "Miscellaneous"
    tag: "misc"

---

{# {% set categories = [""] %}

{% for category in ["poetry", "prose", "essay", "misc"] %}
  {% set categoryPosts = collections["issue" ~ issueNo] | filterPostsByTag(category) %}
  {% if categoryPosts | length > 0 %}
    {% set categoryObj = {"title": category | capitalize, "posts": categoryPosts} %}
    {% set _ = categories.push(categoryObj) %}
  {% endif %}
{% endfor %} #}

{# <div style="display:flex; flex-wrap:wrap; gap:40px">
<img src="{{cover}}" style="width:min(80vw,500px);height:min(80vw,500px);object-fit: cover;">
<section class="med">
    <h1 class="content-title">
        {{title}}
    </h1>

    <ul class="issue-list">
        {%- for post in collections.issue8 -%}
        {% if post.data.tags.includes("misc") %}
            <li>
                <a href="{{post.url}}">{{post.data.title}}
                    /<i>{{post.data.authour}}</i>
                </a>
            </li>
        {%endif%}
        {%- endfor -%}

        <h3 class="issue-catagory">Poetry</h3>
        {%- for post in collections.issue8 -%}
        {% if post.data.tags.includes("poetry") %}
            <li>
                <a href="{{post.url}}">{{post.data.title}}
                    /<i>{{post.data.authour}}</i>
                </a>
            </li>
        {%endif%}
        {%- endfor -%}

        <h3 class="issue-catagory">Prose</h3>
        {%- for post in collections.issue8 -%}
            {% if post.data.tags.includes("prose") %}
                <li>
                    <a href="{{post.url}}">{{post.data.title}}
                        /<i>{{post.data.authour}}</i>
                    </a>
                </li>
        {%endif%}
        {%- endfor -%}
        
        <h3 class="issue-catagory">Essays</h3>
        {%- for post in collections.issue8 -%}
        {% if post.data.tags.includes("essay") %}
                <li>
                    <a href="{{post.url}}">{{post.data.title}}
                        /<i>{{post.data.authour}}</i>
                    </a>
                </li>
        {%endif%}
        {%- endfor -%}
        </ul>        
</section>
</div> #}
